// var - we don't use anymore
let x = 1;
const y = 2; // you MUST initialize const variables
let z; // undefined

x++;
// y++; // error // y = y + 1; // cannot reassign to const variables

console.log( x );
console.log( 'z = ', z );
console.log( undefined ); // apart from this we have null value (used generally as a placeholder for object)